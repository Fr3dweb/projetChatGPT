{% extends 'base.html.twig' %}

{% block title %} Créer Une Catégorie | Projet ChatGpt{% endblock %}
{% block body %}
    <section class="my-5">
        <div class="text-center mt-5">
            <h1 class="text-white">Créer une categorie</h1>
            {{ form_start(form) }}
            {{ form_row(form.name) }}
            {{ form_row(form.submit) }}
            {{ form_end(form) }}
        </div>
    </section>
{% endblock %}